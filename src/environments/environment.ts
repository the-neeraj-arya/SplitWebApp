export const environment = {
    production: true,
    apiBase: 'https://your-production-api.com/api/', // Replace with your production API URL
    firebaseConfig: {
        // Your Firebase configuration for production
    }
};
